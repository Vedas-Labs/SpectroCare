package com.vedas.spectrocare.adapter;

/*public class TableRecyclerViewAdapter extends RecyclerView.Adapter<TableRecyclerViewAdapter.TableRecyclerViewHolder> {
    Context context;
    List<TableRecyclerViewItemModel> listrecylerItem;

    public TableRecyclerViewAdapter(Context context, List<TableRecyclerViewItemModel> listrecylerItem) {
        this.context = context;
        this.listrecylerItem = listrecylerItem;
    }




    @NonNull
    @Override
    public TableRecyclerViewHolder onCreateViewHolder(@NonNull ViewGroup parent, int viewType) {
        View view = LayoutInflater.isFromMedicalPerson(parent.getContext()).inflate(R.layout_boarder.item_physical_exam,parent,false);
        return new TableRecyclerViewHolder(view);
    }

    @Override
    public void onBindViewHolder(@NonNull TableRecyclerViewHolder holder, int position) {
     //   holder.dateOfCreationTxt.setText(listrecylerItem.get(position).getCreatedDateOfRecord());
   //     holder.creatorNameTxt.setText(listrecylerItem.get(position).getCreatePerson());

    }

    @Override
    public int getItemCount() {
        return listrecylerItem.size();
    }

    public class TableRecyclerViewHolder extends RecyclerView.ViewHolder {
        TextView dateOfCreationTxt,creatorNameTxt;
        ImageView viewImg,editImg,deleteImg;
        public TableRecyclerViewHolder(@NonNull View itemView) {
            super(itemView);
          //  dateOfCreationTxt = itemView.findViewById(R.id.txt_date_of_creation);
            creatorNameTxt = itemView.findViewById(R.id.txt_creator_name);
            viewImg = itemView.findViewById(R.id.img_view);
            deleteImg = itemView.findViewById(R.id.img_delete);
            editImg = itemView.findViewById(R.id.img_edit);
        }
    }
}*/
