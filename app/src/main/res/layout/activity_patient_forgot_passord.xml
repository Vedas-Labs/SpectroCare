<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".PatientModule.PatientForgotPassordActivity">
    <ImageView
        android:id="@+id/back_icon"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:src="@drawable/icon_ionic_ios_arrow_back"
        android:padding="20dp"
        android:visibility="visible"
        android:layout_marginTop="15dp"
        android:layout_marginLeft="10dp"/>
    <RelativeLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_centerInParent="true"
        android:layout_marginRight="34dp"
        android:layout_marginLeft="34dp"
        >

        <ImageView
            android:id="@+id/img_done"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:src="@drawable/done"
            android:layout_centerHorizontal="true"
            />

        <TextView
            android:id="@+id/title_change_description"
            android:layout_width="307dp"
            android:layout_height="113dp"
            android:layout_below="@id/img_done"
            android:layout_centerHorizontal="true"
            android:gravity="center"
            android:text="Password reset link has been successfully sent. Please check your email."
            android:textSize="14sp"
            android:textStyle="bold"
            android:fontFamily="@font/montserrat_regular"
            android:textColor="@color/colorDarkBlue"
            android:layout_marginTop="50dp"/>

        <Button
            android:id="@+id/btn_login_now"
            android:layout_width="match_parent"
            android:layout_height="55dp"
            android:fontFamily="@font/montserrat_regular"
            android:text="CHECK NOW"
            android:layout_marginTop="60dp"
            android:textColor="#fff"
            android:backgroundTint="@color/colorDarkBlue"
            style="?android:attr/borderlessButtonStyle"
            android:background="@drawable/new_login_boarder"
            android:layout_below="@id/title_change_description"/>


    </RelativeLayout>

</RelativeLayout>